<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<UserControl
    x:Class="EasePass.Controls.RenamableListViewItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EasePass.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converter="using:EasePass.Converter"
    mc:Ignorable="d" LostFocus="UserControl_LostFocus">
    <UserControl.Resources>
        <ResourceDictionary>
            <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <converter:BoolToVisibilityConverter_Inverted x:Key="BoolToVisibilityConverter_Inverted"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <TextBox Visibility="{x:Bind IsRenaming, Converter={StaticResource BoolToVisibilityConverter}}" Text="{x:Bind Text, Mode=TwoWay}"/>
        <StackPanel Orientation="Horizontal" Visibility="{x:Bind IsRenaming, Converter={StaticResource BoolToVisibilityConverter_Inverted}}">
            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="{x:Bind PWItem.IconId}"/>
            <TextBlock Margin="10,0,0,0" Text="{x:Bind Text, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</UserControl>
