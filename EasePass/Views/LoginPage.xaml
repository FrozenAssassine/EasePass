<Page
    x:Class="EasePass.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EasePass.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock TextAlignment="Center" FontSize="36" Text="Login"/>
        <StackPanel Margin="0,70,0,0" Background="{ThemeResource CardBackgroundFillColorDefault}" CornerRadius="10" Padding="30" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Margin="10,0,0,0" Text="Enter your Password"/>
            <PasswordBox Margin="10" x:Name="passwordBox" MinWidth="150">
                <PasswordBox.KeyboardAccelerators>
                    <KeyboardAccelerator Key="Enter" Invoked="Enter_Invoked"/>
                </PasswordBox.KeyboardAccelerators>
            </PasswordBox>
            <Button Style="{ThemeResource AccentButtonStyle}" HorizontalAlignment="Center" Padding="20,5" Margin="10" Click="PWLogin_Click" Content="Login"/>
        </StackPanel>
    </Grid>
</Page>
