<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Page
    x:Class="EasePass.Views.PasswordsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EasePass.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:EasePass.Models"
    mc:Ignorable="d"
>

    <Grid>
        <NavigationView x:Name="mainNavigationView"
                        PaneDisplayMode="Left"
                        IsTabStop="False"
                        SelectionChanged="mainNavigationView_SelectionChanged">

            <NavigationView.AutoSuggestBox>
                <AutoSuggestBox QueryIcon="Find" SuggestionChosen="passwordSearchBox_SuggestionChosen" TextChanged="passwordSearchBox_TextChanged" x:Name="passwordSearchBox" >
                    <AutoSuggestBox.ItemTemplate>
                        <DataTemplate x:DataType="models:PasswordManagerItem">
                            <TextBlock Text="{Binding DisplayName}"/>
                        </DataTemplate>
                    </AutoSuggestBox.ItemTemplate>
                </AutoSuggestBox>
            </NavigationView.AutoSuggestBox>

            <Grid Margin="0,50,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*" MinWidth="300" MaxWidth="500"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="1" Grid.Row="0" CornerRadius="10" Padding="10" Background="{ThemeResource AccentAcrylicBackgroundFillColorBaseBrush}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
                        <FontIcon Margin="0,20,0,0" x:Name="iconFI" FontFamily="Segoe MDL2 Assets" FontSize="34"/>
                        <TextBlock x:Name="itemnameTB" FontSize="42"/>
                    </StackPanel>
                </Grid>
                <StackPanel MinWidth="200"  Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch" Orientation="Vertical">

                    <TextBlock Margin="0,20,0,0" Text="Username"/>
                    <TextBox x:Name="usernameTB"/>

                    <TextBlock Margin="0,20,0,0" Text="Email"/>
                    <TextBox x:Name="emailTB" />

                    <TextBlock Margin="0,20,0,0" Text="Password"/>
                    <PasswordBox x:Name="pwTB"/>

                    <TextBlock Margin="0,20,0,0" Text="Notes"/>
                    <TextBox AcceptsReturn="True" Height="150" x:Name="notesTB"/>
                </StackPanel>
            </Grid>

        </NavigationView>
    </Grid>
</Page>
