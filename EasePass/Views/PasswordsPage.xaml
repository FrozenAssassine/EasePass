<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Page
    x:Class="EasePass.Views.PasswordsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EasePass.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:EasePass.Models" xmlns:controls="using:EasePass.Controls"
    mc:Ignorable="d"
>
    <Page.Resources>
        <MenuFlyout x:Key="textboxFlyout">
            <MenuFlyoutItem Text="Copy" Click="TB_Copy_Click"/>
            <MenuFlyoutItem Text="Select all" Click="TB_SelectAl_Click"/>
        </MenuFlyout>
    </Page.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Column="0" MinWidth="250">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBox x:Name="searchbox" TextChanged="searchbox_TextChanged" MinWidth="150" Margin="10"/>
            <ListView x:Name="passwordItemListView" SelectionChanged="passwordItemListView_SelectionChanged" Grid.Row="1" ItemsSource="{x:Bind PasswordItems}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:PasswordManagerItem">
                        <controls:RenamableListViewItem Tag="{x:Bind}" PWItem="{x:Bind}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Grid Grid.Column="1" Margin="0,50,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*" MinWidth="300" MaxWidth="500"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <CommandBar Grid.Column="1" Grid.Row="0" OverflowButtonVisibility="Collapsed">
                <CommandBar.PrimaryCommands>
                    <AppBarButton Icon="Add" Label="New item" Click="AddPasswordItem_Click"/>
                    <AppBarButton Icon="Edit" Label="Edit item" Click="EditPasswordItem_Click"/>
                    <AppBarButton Icon="Remove" Label="Delete item" Click="DeletePasswordItem_Click"/>
                </CommandBar.PrimaryCommands>
            </CommandBar>
            <Grid Grid.Column="1" Grid.Row="1" CornerRadius="10" Padding="10" Background="{ThemeResource AccentAcrylicBackgroundFillColorBaseBrush}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
                    <FontIcon Margin="0,20,0,0" x:Name="iconFI" FontFamily="Segoe MDL2 Assets" FontSize="34"/>
                    <TextBlock x:Name="itemnameTB" FontSize="42"/>
                </StackPanel>
            </Grid>
            <ScrollViewer x:Name="passwordShowArea" Visibility="Collapsed" Grid.Column="1" Grid.Row="2" VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto">
                <StackPanel MinWidth="200" HorizontalAlignment="Stretch" Orientation="Vertical">
                    <TextBlock Margin="0,20,0,0" Text="Username"/>
                    <TextBox IsReadOnly="True" ContextFlyout="{StaticResource textboxFlyout}" x:Name="usernameTB"/>

                    <TextBlock Margin="0,20,0,0" Text="Email"/>
                    <TextBox IsReadOnly="True" ContextFlyout="{StaticResource textboxFlyout}" x:Name="emailTB" />

                    <TextBlock Margin="0,20,0,0" Text="Password"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" >
                        <PasswordBox PreviewKeyDown="pwTB_PreviewKeyDown" AllowFocusOnInteraction="True" ContextFlyout="{StaticResource textboxFlyout}" HorizontalAlignment="Stretch" PasswordRevealMode="Hidden" IsPasswordRevealButtonEnabled="False" x:Name="pwTB"/>
                        <CheckBox HorizontalAlignment="Stretch"  Margin="10,0,0,0" IsChecked="False"
                                  Checked="ShowPassword_Changed" Unchecked="ShowPassword_Changed"/>
                    </StackPanel>

                    <TextBlock Margin="0,20,0,0" Text="Notes"/>
                    <TextBox IsReadOnly="True" ContextFlyout="{StaticResource textboxFlyout}" AcceptsReturn="True" Height="150" x:Name="notesTB"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
