<Page
    x:Class="EasePass.Views.PasswordsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Enabled"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:models="using:EasePass.Models" 
    xmlns:controls="using:EasePass.Controls" 
    mc:Ignorable="d"
>
    <Page.Resources>
        <MenuFlyout x:Key="textboxFlyout" x:Name="textboxFlyout">
            <MenuFlyoutItem Text="Copy" Click="TB_Copy_Click"/>
            <MenuFlyoutItem Text="Select all" Click="TB_SelectAl_Click"/>
        </MenuFlyout>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!--Sidebar with searchbox-->
        <Grid Grid.Column="0" MinWidth="250">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBox PlaceholderText="Search" x:Name="searchbox" TextChanged="searchbox_TextChanged" MinWidth="150" Margin="10"/>
            <ListView x:Name="passwordItemListView" SelectionChanged="passwordItemListView_SelectionChanged" Grid.Row="1" ItemsSource="{x:Bind PasswordItems}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:PasswordManagerItem">
                        <controls:RenamableListViewItem Tag="{x:Bind}" PWItem="{x:Bind}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!--About button-->
        <Button Margin="10" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" Click="AboutPage_Click">
            <FontIcon Glyph="&#xE946;" FontFamily="Segoe MDL2 Assets"/>
        </Button>

        <!--Show item grid-->
        <Grid Grid.Column="1" Margin="0,50,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*" MinWidth="400" MaxWidth="500"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--Commandbar-->
            <CommandBar HorizontalAlignment="Center" DefaultLabelPosition="Right" Grid.Column="1" OverflowButtonVisibility="Collapsed">
                <CommandBar.PrimaryCommands>
                    <AppBarButton Icon="Add" Label="New" Click="AddPasswordItem_Click"/>
                    <AppBarButton Icon="Edit" Label="Edit" Click="EditPasswordItem_Click"/>
                    <AppBarButton Icon="Delete" Label="Delete" Click="DeletePasswordItem_Click"/>
                    <AppBarButton Icon="Setting" Label="Settings" Click="Settings_Click"/>
                </CommandBar.PrimaryCommands>
            </CommandBar>
            <!--About button-->
            <ScrollViewer x:Name="passwordShowArea" Visibility="Collapsed" Grid.Column="1" Grid.Row="2" VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto">
                <StackPanel MinWidth="200" HorizontalAlignment="Stretch" Orientation="Vertical">
                    <TextBlock TextAlignment="Center" Grid.Column="1" Grid.Row="1" x:Name="itemnameTB" FontSize="32"/>
                    <TextBlock Margin="0,20,0,0" Text="Username"/>
                    <TextBox RightTapped="tb_RightTapped" IsReadOnly="True" x:Name="usernameTB"/>

                    <TextBlock Margin="0,20,0,0" Text="Email"/>
                    <TextBox RightTapped="tb_RightTapped" IsReadOnly="True" x:Name="emailTB" />

                    <TextBlock Margin="0,20,0,0" Text="Password"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto" MaxWidth="10"/>
                        </Grid.ColumnDefinitions>
                        <PasswordBox RightTapped="tb_RightTapped" Grid.Column="0" PreviewKeyDown="pwTB_PreviewKeyDown" AllowFocusOnInteraction="True" PasswordRevealMode="Hidden" IsPasswordRevealButtonEnabled="False" x:Name="pwTB"/>
                        <CheckBox Content="Show Password" Width="0" Grid.Column="1"  Margin="10,0,0,0" IsChecked="False"
                                  Checked="ShowPassword_Changed" Unchecked="ShowPassword_Changed"/>
                    </Grid>

                    <TextBlock Margin="0,20,0,0" Text="Notes"/>
                    <TextBox RightTapped="tb_RightTapped" IsReadOnly="True" AcceptsReturn="True" Height="150" x:Name="notesTB"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
